This is a fun Number Guessing Game 

The user selects a difficulty level and then types a random number into the input bar

if the Guessed number matches the random number the user wins

To run the game simply fork the repo clone it to your IDE and run npm run dev
