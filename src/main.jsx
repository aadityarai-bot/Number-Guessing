import React from 'react';
import ReactDOM from 'react-dom/client';
import NumberGame from '../Number-game.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <NumberGame />
  </React.StrictMode>
);
